# Future Improvements

Tracking items deferred from sessions that are worth revisiting but not blocking current work.

## Job Tracking

- **Graphical milestone/workflow designer**: Drag-and-drop canvas (Grasshopper-style) to visually define milestone templates, including parallel tracks (e.g., kitchen + bathroom running concurrently, merging at final assembly). Each job references a saved template or uses a default. Deferred: a single flat milestone template suffices for now.
- **Gmail integration**: Auto-pull emails tagged to a job into the Customer bucket. Deferred: manual PDF upload suffices for now.
- **Photo bucket**: Dedicated photo storage for job site or progress photos. Deferred: not needed for now.
