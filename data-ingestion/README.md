# Data Ingestion Module

Transforms physical Job Folder contents (scanned notes, sketches, appliance specs) into structured JSON for the Design Component.

## Next

- Define the JSON schema that bridges Data Ingestion → Design Component
- This is a substantial session of its own (covers job header, rooms, runs, cabinets, material overrides, appliance specs, odds requests, business rules, validation, etc.)
